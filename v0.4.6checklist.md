Below is a **file-by-file checklist** for **v0.4.6**, aligned to the current public repo structure and the roadmap position of **v0.4.6 as a performance release** following **v0.4.5 API hardening**. In the public snapshot I inspected, the `CHANGELOG` still ends at **v0.4.4**, so I have included a documentation backfill step as part of the release checklist. ([GitHub][1])

## Core implementation files

### `src/ramanpl/baselineAPI.py`

* [ ] Extract a shared internal helper for Whittaker-style baselines:

  * [ ] build / reuse difference operator for `(n, diff_order)`
  * [ ] build / reuse `DtD`
  * [ ] centralise weighted sparse solve path
* [ ] Refactor `asls`, `arpls`, and `airpls` to use the shared helper where possible
* [ ] Avoid repeated recomputation of invariant sparse operators inside repeated spectrum calls
* [ ] Keep numerical behaviour unchanged within tolerance
* [ ] Preserve current argument validation and exception messages unless a genuine bug is found
* [ ] Add internal comments explaining which terms are iteration-dependent and which are cacheable

**Programmer note:** this is the highest-priority optimisation target.

---

### `src/ramanpl/preprocessing.py`

* [ ] Review `apply_pipeline_to_mapping_cube(...)` and any cube-level preprocessing helpers
* [ ] Reduce unnecessary `np.asarray(...)`, `ravel()`, reshaping, or temporary allocations where safe
* [ ] Ensure common preprocessing pipelines dispatch efficiently to the optimised native baseline kernels
* [ ] Check whether common fast paths can be made more explicit for:

  * [ ] crop only
  * [ ] crop + Savitzky–Golay
  * [ ] crop + baseline
  * [ ] crop + Savitzky–Golay + baseline
* [ ] Keep preprocessing metadata/provenance behaviour unchanged
* [ ] Do **not** change the public pipeline schema in this build

---

### `src/ramanpl/integration/ramanspy_adapter.py`

* [ ] Audit RamanPL ↔ RamanSPy conversion steps for avoidable copying
* [ ] Standardise array dtype/shape handling before conversion
* [ ] Remove duplicate conversions when the incoming object is already in a compatible cube/spectrum layout
* [ ] Check round-trip conversion cost on representative mapping cubes
* [ ] Add inline comments documenting where conversion overhead still remains unavoidable

---

### `src/ramanpl/integration/ramanspy_bridge.py`

* [ ] Keep backend resolution logic unchanged
* [ ] Check whether bridge-level execution is re-wrapping or re-validating more than necessary
* [ ] Ensure the resolved backend/provenance fields still serialise exactly as before
* [ ] Confirm that performance work here does not alter fallback policy or error semantics

---

### `src/ramanpl/mapping/_preprocess.py`

* [ ] Confirm current preprocessing cache remains intact
* [ ] Verify that optimised native preprocessing is reached through both:

  * [ ] full-cube preprocessing
  * [ ] per-spectrum preprocessing path
* [ ] Check that no redundant full-cube recomputation is triggered during repeated fit/plot/export flows
* [ ] Preserve `self._preprocess_meta`, cache invalidation behaviour, and current fit-normalisation logic

---

### `src/ramanpl/batch.py`

* [ ] Ensure batch workflows benefit automatically from the lower-level preprocessing optimisations
* [ ] Audit repeated fitter construction for any avoidable repeated preprocessing setup
* [ ] Do **not** introduce batch-only shortcuts that bypass the shared preprocessing stack
* [ ] Keep plotting/table/export behaviour unchanged
* [ ] Verify batch export provenance still reflects requested/resolved backend fields correctly

---

## Benchmark and profiling files

### `benchmarks/benchmark_mapping_preprocessing.py`

* [ ] Extend the benchmark from smoke-level comparison to release-level evidence
* [ ] Add benchmark cases for representative workloads:

  * [ ] small cube
  * [ ] medium cube
  * [ ] larger cube
  * [ ] short x-axis
  * [ ] long x-axis
* [ ] Benchmark at least:

  * [ ] crop
  * [ ] Savitzky–Golay
  * [ ] polynomial baseline
  * [ ] asLS
  * [ ] arPLS
  * [ ] airPLS
  * [ ] combined pipelines
* [ ] Record both:

  * [ ] runtime
  * [ ] memory
* [ ] Emit output in a form easy to compare across commits, preferably:

  * [ ] printed summary table
  * [ ] machine-readable JSON or CSV artifact
* [ ] Add a clear “scientific parity not measured here” note so performance and correctness stay separated

---

### `benchmarks/` — **proposed new file:** `benchmark_baseline_kernels.py`

* [ ] Add a microbenchmark focused purely on native baseline routines
* [ ] Benchmark `poly`, `gaussian`, `asls`, `arpls`, `airpls` on controlled 1D spectra
* [ ] Sweep:

  * [ ] spectrum length
  * [ ] iteration count
  * [ ] baseline parameters where meaningful
* [ ] Use this file to confirm whether the largest gain really comes from sparse-operator reuse

---

## Regression and test files

### `tests/test_mapping_backend_benchmark_smoke.py`

* [ ] Update smoke expectations if benchmark output format changes
* [ ] Keep this file lightweight and CI-safe
* [ ] Verify benchmark script still runs without crashing on a minimal case

---

### `tests/test_mapping_memory_runtime_smoke.py`

* [ ] Refresh thresholds only if genuinely required by the new benchmark design
* [ ] Keep thresholds loose enough to avoid brittle CI failures
* [ ] Make sure this remains a smoke/regression guard, not a precision performance contract

---

### `tests/test_mapping_backend_parity.py`

* [ ] Add parity cases for optimised native preprocessing versus pre-optimisation expected behaviour
* [ ] Confirm native/auto/ramanspy backend outputs still satisfy current parity expectations where supported
* [ ] Focus on:

  * [ ] x-axis consistency
  * [ ] cube shape consistency
  * [ ] corrected spectra tolerance
  * [ ] metadata/provenance fields

---

### `tests/test_mapping_cube_consistency.py`

* [ ] Add cases covering cube preprocessing after optimisation
* [ ] Ensure no axis-ordering or shape regressions
* [ ] Verify caching does not produce stale outputs after a pipeline change

---

### `tests/test_export_provenance_regressions.py`

* [ ] Re-run provenance checks after preprocessing-path optimisation
* [ ] Confirm export headers/metadata are unchanged for:

  * [ ] mapping
  * [ ] batch
  * [ ] native
  * [ ] auto
  * [ ] ramanspy
* [ ] Ensure no new internal cache keys or profiling details leak into exported metadata

---

### `tests/` — **proposed new file:** `test_baseline_numerical_parity.py`

* [ ] Add direct 1D tests for baseline methods before/after optimisation
* [ ] Compare corrected signal and baseline arrays within explicit tolerances
* [ ] Include:

  * [ ] no-mask case
  * [ ] masked case
  * [ ] small-spectrum edge case
  * [ ] high-iteration case
* [ ] Include one test per method for invalid parameter handling

---

### `tests/` — **proposed new file:** `test_baseline_operator_cache.py`

* [ ] If you implement internal sparse-operator reuse, test cache correctness directly
* [ ] Verify cache keying works for:

  * [ ] different `n`
  * [ ] different `diff_order`
  * [ ] repeated calls
* [ ] Confirm cache reuse does not change returned results
* [ ] Keep this as an internal-behaviour test only if the cache is deterministic and worth locking down

---

### `tests/` — **proposed new file:** `test_batch_performance_path_regressions.py`

* [ ] Confirm batch results remain identical after lower-level optimisation
* [ ] Ensure batch still uses the standard fitter/preprocessing path
* [ ] Cover:

  * [ ] fit output
  * [ ] long-format parameter table
  * [ ] waterfall / overlay return signatures
  * [ ] metadata inference

---

## Documentation and release files

### `README.md`

* [ ] Update roadmap progress after implementation
* [ ] Add a short v0.4.6 summary under the v0.4.x section:

  * [ ] what was optimised
  * [ ] what was deliberately not changed
  * [ ] whether gains are strongest for native, RamanSPy, or both
* [ ] If benchmark usage changed, document how to run the benchmark scripts
* [ ] Do **not** advertise unsupported workflows as improved unless you actually measured them

---

### `CHANGELOG`

* [ ] Backfill **v0.4.5** if your local branch has completed work that is not yet documented in the public changelog
* [ ] Add a **v0.4.6** entry with sections such as:

  * [ ] Added
  * [ ] Changed
  * [ ] Optimised
  * [ ] Tests
* [ ] Record performance claims conservatively
* [ ] Mention explicitly that scientific behaviour and public API were preserved if that is true after testing

---

## Recommended implementation order

1. `baselineAPI.py`
2. `benchmark_baseline_kernels.py` *(new)*
3. `preprocessing.py`
4. `ramanspy_adapter.py`
5. `mapping/_preprocess.py`
6. `batch.py`
7. parity/regression tests
8. benchmark script finalisation
9. `CHANGELOG`
10. `README.md`

## Release gate for v0.4.6

* [ ] Benchmarks show a real improvement on representative workloads
* [ ] Native baseline parity tests pass
* [ ] Mapping/batch/export regression tests pass
* [ ] No public API/schema change slipped in
* [ ] `CHANGELOG` and `README.md` are aligned with the actual implementation

The current public repo layout, existing benchmark harness, mapping cache layer, optional RamanSPy backend split, and v0.4.6 roadmap scope all support this prioritisation. ([GitHub][1])

[1]: https://raw.githubusercontent.com/barry063/RamanPL_2D/main/README.md?utm_source=chatgpt.com "raw.githubusercontent.com"
